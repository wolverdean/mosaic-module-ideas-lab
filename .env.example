# Inherited from Mosaic framework — set in the framework's .env, not here.
# Shown here for reference when running the module standalone in tests.

UPLOAD_DIR=./uploads

# Optional: per-module env vars (prefix: MOSAIC_IDEAS_LAB_)
# MOSAIC_IDEAS_LAB_MAX_ATTACHMENT_MB=25
